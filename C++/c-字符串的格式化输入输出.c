------------------------
字符串常量				|
------------------------
	# 字符串是内存中一段连续的char空间,以'\0'(数字0)结尾
	# 字符串常量是由双引号括起来的字符序列
	# 每个字符串的结尾,编译器会自动添加一个结束位标志'\0'

------------------------
字符的输入				|
------------------------
	# 单个字符
		char ch;
		scanf("%c",&ch);
		printf("%c",ch);

		char ch = getchar();
		printf("%c",ch);
		