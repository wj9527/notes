--------------------------------
stdlib							|
--------------------------------

exit()
	* 程序退出,参数为 int 值,表示程序的退出状态

system()
	* 执行系统命令
	* 返回值 int,该值就是程序的返回值,在不同的平台不一样

srand(unsigned int)
	* 设置随机数的种子

rand()
	* 该函数返回一个随机数 int
	* 如果种子一样,那么多次调用的结果也一样


fclose()
fopen()
freopen()
fflush()

FILE

NULL
	* null 

EXIT_SUCCESS = 0
	* 成功

EXIT_FAILURE = 1
	* 失败
	
