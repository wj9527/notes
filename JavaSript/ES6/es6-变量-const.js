--------------------
const				|
--------------------
	# 有点像java中的 final,值不允许被改变,如果进行改变,会抛出异常
	# const 一旦声明变量，就必须立即初始化，不能留到以后赋值。
	# const 的作用域与let命令相同：只在声明所在的块级作用域内有效。
	# const 命令声明的常量也是不提升，同样存在暂时性死区，只能在声明的位置后面使用。
	# const 声明的常量，也与let一样不可重复声明。
	# ES6 声明变量的六种方法	
		var 
		function
		let
		const
		import
		class
